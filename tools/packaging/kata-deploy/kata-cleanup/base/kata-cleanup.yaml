---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kubelet-kata-cleanup
  namespace: kube-system
spec:
  selector:
    matchLabels:
      name: kubelet-kata-cleanup
  template:
    metadata:
      labels:
        name: kubelet-kata-cleanup
    spec:
      serviceAccountName: kata-deploy-sa
      hostPID: true
      nodeSelector:
        katacontainers.io/kata-runtime: cleanup
      containers:
        - name: kube-kata-cleanup
          image: quay.io/fidencio/kata-containers:3.9.0-exposing-config-fs
          imagePullPolicy: Always
          command: ["bash", "-c", "/opt/kata-artifacts/scripts/kata-deploy.sh reset"]
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          securityContext:
            privileged: true
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 1
    type: RollingUpdate
